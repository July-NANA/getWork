#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    vector<bool> v(6, 0);
    for (vector<bool>::iterator it = v.begin(); it < v.end(); it++)
    {
        /* code */
        cout << (*it) << endl;
    }

    return 0;
}
